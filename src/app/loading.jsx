import React from 'react';

const Loading = () => {
    return (
        <div>
            <h1 className="text-3xl font-bold text-red-600 text-center">Loading......</h1>
        </div>
    );
};

export default Loading;